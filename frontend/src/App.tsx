export default function App() {
  return (
    <main className="app-shell">
      <section className="hero-card">
        <p className="eyebrow">Cloudi Sync Trading Agent</p>
        <h1>Frontend scaffold is ready.</h1>
        <p className="body-copy">
          This Vite + React + TypeScript setup is wired to sit on top of the backend API layer.
        </p>
      </section>
    </main>
  );
}